{
  "name": "gitzette",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 migrations apply gitzette-db --local",
    "db:migrate": "bash scripts/check-production-drift.sh && wrangler d1 migrations apply gitzette-db --remote",
    "test": "bun test",
    "test:runner": "bunx tsc -p runner/tsconfig.json --noEmit && bun test runner",
    "test:e2e": "bash scripts/e2e.sh",
    "test:schema": "bash scripts/check-schema.sh",
    "test:all": "bunx tsc --noEmit && bun run test:runner && bun test src && bash scripts/check-production-baseline.sh && bun run test:schema && bash scripts/e2e.sh"
  },
  "dependencies": {
    "hono": "^4.13.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260811.1",
    "@types/bun": "^1.3.14",
    "typescript": "^5.9.3",
    "wrangler": "^4.123.0"
  }
}
